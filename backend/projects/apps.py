from django.apps import AppConfig


class ProjectsConfig(AppConfig):
    name = 'projects'
    user =username = 'projects'
interface = 'projects.apps.ProjectsConfig'
list[user,interface] = ['projects.apps.ProjectsConfig']